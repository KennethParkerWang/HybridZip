# HybridZip R2 Original Baseline

## Identity

- Baseline tag: `baseline-r2-20260828`
- Snapshot date: 2026-08-28 (Asia/Shanghai)
- Branch: `main`
- Release executable: `build/Release/hybridzip.exe`
- Release executable SHA-256: `CC6DA8404E3A2789A0E98BED460C4FAA90822BAC0EA362C67E261774BD0BF191`

This tag records the current working HybridZip R2 implementation before the
next router, zstd fast-path, and throughput work. It is a development baseline,
not a claim that the PAQ8px ratio, Tencent-corpus, CPU-throughput, or GPU
targets have been achieved.

## Included State

- HZ01 / `PROFILE_V1` compatibility path.
- HZ02 modes `0..42` and current decoder-visible routing.
- Current R2 source, donor provenance, architecture figures, research packet,
  and experiment tooling.
- Current-hash ledger metadata and strict analysis for
  `hybridzip-r2-currenthash-cc6d-20260827-r2`:
  44 packages, 528/528 `COMPLETE/PASS` rows, 12 Silesia files, leading 32 KiB
  prefixes, Auto archive total 99,720 bytes, and 2.028809 bpb.

## Deliberately Excluded

- Build outputs under `build/`.
- Raw experiment `inputs/`, `archives/`, `decoded/`, and `logs/` directories.
- Local smoke diagnostics, temporary `.out`/`.err` files, and Python caches.
- External corpora under `E:\MIXER\KU` or `F:\paq8px`.

The excluded files remain on the local workspace and are not deleted by this
baseline operation. Their provenance and local paths are documented in
`docs/SOURCES.md`, `docs/PRODUCT_STATUS.md`, and the R2 research packet.

## Verification Boundary

The snapshot was checked with `git diff --check`. The existing current-hash
ledger remains the authoritative runtime evidence; this tag itself does not
start a new codec process or alter any archive bytes.
